---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: service-centos-latest
    image: centos:latest
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        service_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: centos-latest
